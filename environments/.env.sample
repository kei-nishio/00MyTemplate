# プロジェクト設定
SITE_TITLE=template # WordPress site title (project name)
THEME_NAME=template_theme # WordPress theme file name
LOCAL_SITE_DOMAIN=template.local # WordPress Local Site Domain

# ビルド設定
EJS_MODE=true # EJSの場合はtrueにする（静的コーディングのみの場合はfalse）
WP_MODE=false # WordPressの場合はtrueにする（静的コーディングのみの場合はfalse）
WP_LOCAL_MODE=false # WordPressLocalの内容を上書きする場合はtrueにする

# ディレクトリ設定
SRC_EJS_DIR=../src/ejs # EJSファイルのディレクトリ

# 画像圧縮設定
JPEG_QUALITY=80 # JPEG圧縮品質（0〜100）

# ブラウザサポート設定
BROWSERS=last 2 versions,> 5%,ie >= 11,not ie <= 10,ios >= 8,and_chr >= 5,Android >= 5 # サポートブラウザの設定（Browsers_list形式）

# 本番サーバー設定（SFTP）
PRODUCTION_DEPLOY=false # 本番サーバーへのデプロイを有効にする場合はtrueにする
PRODUCTION_HOST=example.com # 本番サーバーのホスト名
PRODUCTION_PORT=22 # SSHポート番号
PRODUCTION_USER=username # SSHユーザー名
PRODUCTION_PRIVATE_KEY_PATH=~/.ssh/id_rsa # SSH秘密鍵のパス
PRODUCTION_SITE_ROOT=/home/username/public_html/site_name # サイトルート（/wp-content/themes/テーマ名は自動付与）